<snippet>
    <content><![CDATA[
////////////////////////////////////////////////////////////////////////////////////////////////////////////   the .h

${1:${2:struct ForwardDeclaration1;
class ForwardDeclaration2;}

${3:namespace internal
{
${4:class ForwardDeclaration3;}
\} // internal}}


    /**
     * ${13:TODO doc}.
     */
    ${5:ErrorCode} ${6:function(  )};



//////////////////////////////////////////////////////////////////////////////////////////////////////////// the .cpp

// -------------------------------------------------------------------------------------------------

${5:ErrorCode} ${7:ClassName}::${6:function( )}
{
    ${8:auto impl = m_impl;
    if ( !impl )
    {
        return CARLO_ERROR_NOT_VALID;
    \}

    return impl->${6:function( )};${9: //< adjust function calls!}}
}



//////////////////////////////////////////////////////////////////////////////////////////////////////////// the impl.h

${10: #include <carlo/store/IncludeFile1.h>}


    ${5:ErrorCode} ${6:function( )};



//////////////////////////////////////////////////////////////////////////////////////////////////////////// the impl.cpp

${11: #include <carlo/store/IncludeFile2.h>}


// -------------------------------------------------------------------------------------------------

${5:ErrorCode} ${7:ClassName}Impl::${6:function( )}
{
    ${14:// TODO}
${12:
    return CARLO_ERROR_NONE;}
}

]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>PIMPL-Function-Snippet</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <!-- <scope>source.python</scope> -->
</snippet>
